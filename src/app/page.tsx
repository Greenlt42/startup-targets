export default function Home() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-semibold">Startup Targets</h1>
      <p className="mt-2 text-sm text-neutral-500">
        Dashboard not yet built — targets table lives in Supabase (see supabase/schema.sql).
      </p>
    </main>
  );
}
